# Experiment fixtures

Place only sanitized deterministic inputs shared by compiled experiment tests here. The current PF1 tests construct minimal plugin trees under their repository-local artifacts output, so no binary or private fixture is committed.
